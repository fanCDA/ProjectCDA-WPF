<Window x:Class="ProjectCDA.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:sys="clr-namespace:System;assembly=mscorlib"
        xmlns:localData="clr-namespace:ProjectCDA.Data"
        xmlns:UserControls="clr-namespace:ProjectCDA.UserControls"
        mc:Ignorable="d"
        Title="MainWindow">

    <Window.DataContext>
        <localData:DataSource />
    </Window.DataContext>

    <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
        <Border Background="LightGray" BorderBrush="Red" BorderThickness="1">
            <ItemsControl VerticalAlignment="Top" ItemsSource="{Binding Schedule}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <UserControls:TwoPagesTemplateControl />
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <UniformGrid Columns="8" />
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
            </ItemsControl>
        </Border>
    </ScrollViewer>
</Window>
